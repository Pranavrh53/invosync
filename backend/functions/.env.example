# InvoSync Backend Environment Variables

# Firebase Configuration
# You can get these values from your Firebase project settings > Service Accounts
# Option 1: Use Environment Variables (Recommended for Security)
FIREBASE_PROJECT_ID=invosync-ac500
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@invosync-ac500.iam.gserviceaccount.com
# Note: For the private key, copy the entire key including -----BEGIN PRIVATE KEY----- and \n characters
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQD...\n-----END PRIVATE KEY-----\n"

# Option 2: Use Service Account File
# Place your serviceAccountKey.json in the root of the backend folder (d:\invosync\backend)
# The system will automatically look for it if env vars are not set.

# Server Configuration
PORT=3000
NODE_ENV=development

# Backup Configuration
BACKUP_DIR=./backups

# Razorpay Configuration (Test/Sandbox Mode)
# Get test keys from: https://dashboard.razorpay.com/app/keys
# IMPORTANT: Use TEST keys only, NOT live keys
RAZORPAY_KEY_ID=rzp_test_RiZPzSfOiggdN
RAZORPAY_KEY_SECRET=SSpLzqpZ4K53Yas9dkjgzzeC

# Frontend URL (for payment callbacks)
FRONTEND_URL=http://localhost:5173

# ============================================
# HOW TO GET FIREBASE CREDENTIALS
# ============================================
# 1. Go to Firebase Console > Project Settings > Service accounts
# 2. Click "Generate new private key"
# 3. Open the downloaded JSON file
# 4. Copy the "client_email" and "private_key" values to the variables above
# ============================================
